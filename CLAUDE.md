# CLAUDE.md

## Go Binaries

Never commit compiled Go binaries to this repository. When building Go binaries, output them to a directory outside the repo (e.g. `/tmp/`) or use a gitignored directory. Never place them in the repo root or any tracked directory.
